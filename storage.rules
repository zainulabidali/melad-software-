rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /institutes/{instId}/{allPaths=**} {
      allow read: if true; // Public access for result portals, live screens, posters & ID card exports
      allow write: if request.auth != null; // Restricted to authenticated admin sessions
    }
  }
}
